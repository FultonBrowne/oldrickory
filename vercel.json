{
    "builds": [{
        "src": "oldrickory/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "oldrickory/wsgi.py"
        }
    ]
}